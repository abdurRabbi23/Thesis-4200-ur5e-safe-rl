# Run log — UR5e Safe-RL Thesis (clean restart)

One dated line per training run or notable event. Newest at the bottom.
Deep context lives in `logbook/NN_*.md`; this file is the timeline only.

Format: `YYYY-MM-DD | Day N | module | what happened | result / where logged`

---

2026-07-27 | Day 0 | setup | Clean restart. New working folder created at ~/Abdur_Rabbi_Thesis_updated. Skeleton, PROJECT_INSTRUCTIONS.md, CLAUDE.md, logbook and Thesis_Documentation stubs written. Landmine list carried over from the archive into 07_Troubleshooting.md. No code, no configs, no results carried over. | skeleton only, nothing measured

2026-07-27 | Day 1 | 01_env_setup | Stack verified on lab PC: torch 2.7.0+cu128 / CUDA True / numpy 1.26.0 / RTX 5090 / driver 580.173.02 (drift from recorded 580.159.03). Existing `isaaclab` conda env reused — Isaac Sim 5.0.0.0 already present. | all §7 values match except driver; §7 corrected

2026-07-27 | Day 1 | 01_env_setup | Isaac Lab cloned into this folder and pinned to **tag v2.3.0** (branch `frozen/2.3.0`, HEAD 3c6e67bb5) — not the `release/2.3.0` branch, which carries the URDF-importer 2.4.31 startup crash. `./isaaclab.sh -i` run; all five isaaclab* editable installs resolve to this folder, rsl-rl-lib 3.0.1. | clone verified; archive-code import risk cleared

2026-07-27 | Day 1 | 01_env_setup | Cartpole smoke test, `Isaac-Cartpole-v0 --headless`. | PASS — 150 iters, mean ep length 300.00, time_out 0.9988, 16 s. Log: logbook/01_smoke_cartpole.log

2026-07-27 | Day 1 | 01_env_setup | Franka Reach validation, `Isaac-Reach-Franka-v0 --headless --num_envs 4096 --max_iterations 100`. | PASS — 0 NaNs, position_error 0.2702 → 0.0919 m monotonic, reward −2.14 → −0.49 rising, 0.24 s/iter ≈ **4.2 it/s** (archive recorded 2.44 — superseded). Log: logbook/01_smoke_franka_reach.log

2026-07-27 | Day 1 | 01_env_setup | TensorBoard `--bind_all` reachable from laptop at 100.109.10.66:6006; both runs render. | PASS

2026-07-27 | Day 1 | 01_env_setup | False alarm investigated and closed: Kit writes to `Kit/Isaac-Sim/5.1/` while pip reports Isaac Sim 5.0.0.0. Archive logs show the same path across working runs; archive logbook records it as the 5.1 *asset library* vs 5.0 sim. pip is authoritative. | benign — logged in 07_Troubleshooting.md §5

2026-07-27 | Day 1 | 01_env_setup | **Module 01 COMPLETE.** All six gates green. Next: Module 02 UR5e ArticulationCfg. | ✅

2026-07-27 | Day 1 | git | First real push failed: remote was `https://` and GitHub no longer accepts password auth. `ssh -T git@github.com` confirmed the key works; `git remote set-url origin git@github.com:abdurRabbi23/Thesis-4200-ur5e-safe-rl.git` fixed it. | pushed — commit d5f4b47, `[new branch] main -> main`, 52 objects. §5 corrected (it claimed SSH)

2026-07-27 | Day 2 | 02_grasp_env | STEP 2 — asset probe. `probe_ur5e_asset.py` listed the Nucleus UniversalRobots folder with the shipped UR10e path as a reachability CONTROL. | RESOLVED Option A — `ur5e/ur5e.usd` exists on Nucleus (alongside ur3/ur3e/ur5/ur10/ur10e/ur16e/ur20/ur30). No URDF import needed. Log: logbook/02_probe_ur5e_asset.log

2026-07-27 | Day 2 | infra | **New landmine.** Probe run 1 logged the Isaac Sim banner then stopped dead — zero script output. Cause: Isaac Sim dies inside `simulation_app.close()` and Python stdout is block-buffered through `tee`, so the buffer is discarded. Also `$?` after a pipe reports tee (always 0), not the script. | Fixed by `PYTHONUNBUFFERED=1` + `sys.stdout.reconfigure(line_buffering=True)`; use `${PIPESTATUS[0]}`. Logged in 07_Troubleshooting.md

2026-07-27 | Day 2 | 02_grasp_env | STEP 3 — wrote `ur5_grasp/robots/ur5e_cfg.py`, arm only, modelled on **UR10e_CFG** (three actuator groups), not UR10_CFG. Gravity kept ON, against the UR10e reference, matching FRANKA_PANDA_CFG — the analogue for joint-position RL, and the honest choice for Layer 3. | cfg written; effort limits deliberately left to the USD

2026-07-27 | Day 2 | 02_grasp_env | STEP 4 run 2 — `check_ur5e.py`, UR10e gains verbatim. 8/9 PASS. Elbow steady-state sag 0.026703 rad (1.53°) vs 0.0089 elsewhere; `shoulder_pan` 0.000080 confirms physics (vertical axis, no gravity torque). τ = k·err gives elbow 16.0 N·m — largest load, weakest gain (600 vs shoulder 1320), inherited from the heavier UR10e. | FAIL on drift tolerance only. Diagnosis: under-gained elbow, not instability

2026-07-27 | Day 2 | 02_grasp_env | STEP 4 run 3 — **one knob**: elbow stiffness 600 → 1320. Prediction recorded before the run: 0.01214 rad. | **PASS 9/9.** Measured 0.011847 (−2.4%, explained by τ falling 16.02 → 15.64 N·m). All other joints unchanged to 4 dp. EE +7 mm in z vs +6.7 predicted. `τ = k·err` now calibrated. Log: logbook/02_check_ur5e.log

2026-07-27 | Day 2 | 02_grasp_env | Archive inspected on request ("robot with a table"). **The table is Isaac Lab stock** — `lift_env_cfg.py:45`, SeattleLabTable at [0.5,0,0]; the archive inherited it via `LiftEnvCfg` and so will we. Nothing to import. Archive's own work is the gripper; `ur5e_rhp12.usd` is a 2 KB reference stub, not a model. Archive `00_INDEX.md` Day 17: its Layer 1 headline rests on a **proximity weld**, cPPO never run on RH-P12-RN. | Decision: copy `rh_p12_rn/` URDF+meshes as third-party source, rebuild USD + mount here, re-measure every geometric number

2026-07-27 | Day 2 | 02_grasp_env | **ARM SIGNED OFF.** UR5e ArticulationCfg loads clean: 6 joints correct order, 7 bodies, fixed base, effort 150/150/150/28/28/28 N·m and vel 3.142 rad/s both matching the UR5e datasheet, no ArticulationRootAPI error, home pose held. EE at x=0.433 y=0.133 z=0.473 m. | ✅ Next: gripper (RH-P12-RN)

**2026-07-28 (Day 3)** — Scope change agreed: **one UR5e, two selectable grippers** (RH-P12-RN +
Robotiq 2F-85), both really actuated. Driver is optionality + literature comparability, *not* lab
hardware — so the 2F-85 is a bonus, RH-P12-RN stays the Layer 1 critical path. §9's rejection
re-opened on one condition: a shipped, already-coupled USD must exist. Wrote
`ur5_grasp/scripts/probe_gripper_assets.py` (not yet run) — checks the stock `ur5e.usd` for a
`Gripper` variant set, lists Nucleus gripper folders, and inspects any candidate's variants, with
the ur10e path as control. Switch design decided: two built USDs + a `GRIPPERS` registry behind a
`--gripper` flag; separate task ids and `experiment_name` per gripper. 2F-85 timeboxed to ONE day.
Verified by grep on our own copy: RH-P12-RN URDF has 4 revolute joints, **0 mimic tags** — pure
tree, independently confirming the archive diagnosis. Open question logged: sim gives 4 drivable
finger joints where the real RH-P12-RN is 1-DOF. Detail in `logbook/02_grasp_env.md`.

2026-07-28 | Day 3 | 02_grasp_env | STEP 1 — copied `ur5_grasp/assets/rh_p12_rn/` from the archive as third-party source: 7 files, 88 K (URDF + LICENSE_ROBOTIS + 5 STL), MD5-identical. **No `.usd` and no calibrated number copied.** | done

2026-07-28 | Day 3 | 02_grasp_env | STEP 2 — read the archive's `make_ur5e_rhp12_usd.py` for METHOD only, then wrote our own `ur5_grasp/tools/make_ur5e_rhp12_usd.py`. Method carried over: convex-decomposition colliders, `Gripper=None` variant, strip the nested articulation root, fixed mount joint. Departures: no borrowed numbers, cube edge measured not hardcoded, PASS/FAIL criteria written into the script, report to `logbook/`. | script written

2026-07-28 | Day 3 | 02_grasp_env | STEP 3 run 1 — build + validate. **Mount works: 10 joints / 12 bodies, ONE articulation**, nested articulation root stripped at `/Robot/RHP12/rh_p12_rn_base`. Stroke sweep monotonic, origin gap 0.1145 → 0.0216 m — **matches the archive's table to 4 dp**, an independent replication of the geometry. | PASS on topology; two measurements wrong (below)

2026-07-28 | Day 3 | 02_grasp_env | **My bug, not Isaac's.** `measure_pad_half_thickness` used `size/2` and projected it — correct only if the AABB is centred on the body origin, which it is not (the origin sits on the joint axis). Symptoms: open clear opening 92.4 mm against a published 106 mm, and an impossible **negative** face gap (−0.0004 m) at full close. | Diagnosed before re-running

2026-07-28 | Day 3 | 02_grasp_env | STEP 3 run 2 — **one knob**: AABB half-size → true AABB support, and the two pads measured in OPPOSITE directions. Predictions recorded before the run. | **7/7 PASS.** face_gap q=0 **0.1064 m** (predicted 0.1067), q=1.00 **0.0137 m** (predicted 0.0138), pad reach 0.0041+0.0040 = **0.0081 m** (predicted 0.0078). Control held: origin_gap and TCP byte-identical to run 1. Log: `logbook/02_make_ur5e_rhp12.log`

2026-07-28 | Day 3 | 02_grasp_env | **External validation.** Open clear opening **106.4 mm** vs the ROBOTIS published stroke **106.0 mm** (+0.4 mm). Source: emanual.robotis.com/docs/en/platform/rh_p12_rn/ (stroke cut 109 → 106 mm from 2019-11-04 for fingertip durability). First geometry number in this thesis validated against a source outside the project. | ✅ datasheet check added as a permanent acceptance criterion

2026-07-28 | Day 3 | 02_grasp_env | Wrote `ur5_grasp/tools/inspect_usd_geometry.py` — one diagnostic for two open problems, because both are USD **instancing** questions. Walks a stage with and without `Usd.TraverseInstanceProxies()` and reports bounds per purpose, extent hints and raw mesh points. | run on cube + gripper

2026-07-28 | Day 3 | 02_grasp_env | **Cube edge settled by physical measurement.** USD reading was ambiguous (visuals-after-transform 0.060, collision extent 0.060, but `purpose=guide` gave a nonsensical 0.0036). Drop test on a plane at z=0: raw cube rests at **0.03000 m** → edge **0.06000 m**; at env scale 0.8 rests at 0.02400 → **0.04800 m**. Scaling exactly linear. Both at rest (|v| = 0.00012 m/s). | **The archive's 0.0412 m is WRONG by 8.5 mm (raw).** My own prior prediction of 0.0515 also refuted. Log: `logbook/02_measure_dexcube.log`

2026-07-28 | Day 3 | 02_grasp_env | **Consequence — archive TCP_OFFSET 0.130 is invalidated.** Its justification ("pad faces close to 0.0415 m against a 0.0412 m cube, delta +0.3 mm — a true flat-pad parallel grip") is built on a cube that is really 0.048 m. Pads cannot close to 0.0415 around a 0.048 m cube. **Do not carry 0.130 over.** Concrete vindication of §14; worth a paragraph as a negative result. | archive grasp calibration rejected

2026-07-28 | Day 3 | 02_grasp_env | **Grasp-test prediction derivable now.** Interpolating our own face_gap column at the measured 0.0480 m cube: pads should stall at **q ≈ 0.69**. Later = crushing or slipping; earlier = wedging on the curved r1/l1 links. Written before the run. | prediction banked

2026-07-28 | Day 3 | 02_grasp_env | **Second wrong diagnosis of my own, caught.** `colour_gripper` reported "0 renderable prims" and its warning blamed a failed reference. `inspect_usd_geometry.py` showed the truth: all 10 finger meshes are USD **instances** (`/__Prototype_1..10`) and `Usd.PrimRange` does not descend into instance proxies. Confirmed the same for the DexCube (2 instances). | GUI check: **hand renders black** — the ancestor material binding already reaches the proxies, because it is authored outside the prototypes

2026-07-28 | Day 3 | 02_grasp_env | Removed the dead per-mesh `displayColor` loop rather than fixing it — instance proxies and prototypes are both READ-ONLY, and the root binding already works. Mesh count now taken through `TraverseInstanceProxies()`, so a zero really would mean a failed reference. | script corrected; no re-run needed

2026-07-28 | Day 3 | infra | **New landmine.** `Usd.PrimRange(prim)` silently skips instance proxies. Any traversal that authors or counts geometry on an imported asset will find nothing and report success. Both Isaac props and URDF-converted assets are instanced by default. | logged for `07_Troubleshooting.md`

2026-07-29 | Day 4 | 02_grasp_env | Wrote `ur5_grasp/robots/ur5e_rhp12_cfg.py`. Arm actuators carried from the MEASURED `ur5e_cfg.py` (shoulder 1320 / elbow 1320 / wrist 216), **not** the archive's blanket 800/40. New vs the arm cfg: `activate_contact_sensors=True` (something finally reads it), solver position iterations 16 → 32 (this articulation presses pads into a rigid cube). | cfg written, not yet run

2026-07-29 | Day 4 | 02_grasp_env | **Day 3 open question answered: the gripper is ONE degree of freedom.** All four finger joints share one actuator group and one scalar target. Reason: the real RH-P12-RN has a single Dynamixel and coupled fingers, so a policy on 4 free joints would learn grasps the hardware cannot execute and Layer 3 would transfer a robot that does not exist. Evidence the same-q coupling is the *right* one, not just convenient: the Day 3 sweep used it and reproduced the published 106.0 mm stroke to +0.4 mm. Costs nothing — `BinaryJointPositionAction` gives a scalar action space either way. | decision recorded

2026-07-29 | Day 4 | 02_grasp_env | **Grip force sourced externally.** ROBOTIS publishes Maximum Gripping Force **170 N** (also 5 kg payload, 75 mm/s closing, 500 g) — emanual.robotis.com/docs/en/platform/rh_p12_rn/. The URDF's `effort="1000"` is a placeholder worth ~17 kN at the pad; under it any "stall width" would be a fact about PhysX, not a gripper. Set `effort_limit_sim = 10.0 N-m` (170 N × ~0.06 m lever). The lever is an estimate, so **the force is validated, not the torque** — the test prints the achieved force and the exact factor to land on 170 N. Second external validation in this thesis, after the 106.4 mm stroke check. | number sourced, pending measurement

2026-07-29 | Day 4 | 02_grasp_env | Wrote `ur5_grasp/scripts/grasp_hold_test.py`. Deliberately **no IK**: arm at the Day 2 home pose, cube teleported to the pad midpoint and pinned kinematically while the fingers close, then released. Rationale: with a reach in the loop, a failure could be the grasp or the IK — and the TCP frame IK needs is what this script measures. Circular. Measures stall q, pad-to-cube contact force (filtered on the cube, so it is not finger-on-finger), static hold drop, slip during a lift arc, and TCP_OFFSET at the grasp. | script written, not yet run

2026-07-29 | Day 4 | 02_grasp_env | **Five predictions banked before the run**: stall q 0.69±0.03; peak pad force 100–300 N; static drop < 5 mm; lift slip < 5 mm (**the genuine unknown — the wedge is exactly what a static test cannot see**); TCP_OFFSET ≈ 0.1015 m. Extended decision-rule table written for the new contact-sensor symptom class, including the two ways a force can read a false zero. Detail in `logbook/02_grasp_env.md`. | rules written before the run, per §5

2026-07-29 | Day 4 | 02_grasp_env | **Run 1: FAIL 3/8.** Header clean — cube 0.04800 m, effort 10.0 N-m, contact sensing True, both pad prims resolved 2/2, gripper joints found by name at indices [7,6,9,8] (tree-depth reordering, as warned). Real observation: the fingers ARE mechanically blocked — lag grows monotonically to **0.2561 rad**, origin gap floors at ~0.079 m and oscillates. **But it is not a grasp: minimum face gap 0.0651 m against a 0.0480 m cube, 17.1 mm too wide.** Log: `logbook/02_grasp_hold_test.log` | 3 test-rig bugs found, all mine

2026-07-29 | Day 4 | 02_grasp_env | **Bug 1, and the worst one: the stall detector required lag AND force at once.** Force never registered, so a real mechanical stall printed as "pads never stalled" while the same table showed 0.256 rad of lag. A broken sensor reported as a robot result — same class as §9's "it ran without crashing". Fixed: lag and force detected and reported separately. | detector corrected

2026-07-29 | Day 4 | 02_grasp_env | **Bug 2: TCP_OFFSET computed across two instants.** `grasp_mid` captured at the end of PHASE 2, wrist pose read in PHASE 5 — after PHASE 4 moved the arm 84.2 mm and 0.3 rad. Reported 0.2258 m against a Day 3 expectation of 0.0767–0.1049 m. Pure bookkeeping. Fixed: wrist pose captured at the grasp. | **the 0.2258 m TCP_OFFSET is void — do not cite it**

2026-07-29 | Day 4 | 02_grasp_env | **Bug 3 (hypothesis, NOT yet confirmed): the cube was placed in the throat of the hand.** `mid0` is the midpoint of the two pad BODY ORIGINS, and a body origin sits on its joint axis — at the base of the fingertip, not on the gripping face. That parks the cube between the curved r1/l1 links, exactly where Day 3 predicted the wedge. Leading explanation for the 17 mm early blockage. | to be tested, not assumed

2026-07-29 | Day 4 | 02_grasp_env | Unexplained and instrumented rather than guessed at: the pinned cube settled **8.78 mm** off command, almost all −z. One step of gravity at dt=1/120 is 0.34 mm, so that is ~5 steps of free fall — the pin is not holding every step. Per-step tracking error now printed. | diagnostic added

2026-07-29 | Day 4 | 02_grasp_env | PHASE 3+4 read together: cube dropped 10.11 mm on release then **stopped**, and tracked the TCP through an 84.2 mm lift to within **0.06 mm**. Something held it rigidly — a wedge would do that, and so would a grasp. The force channel is what separates them, and the force channel is what failed. | the question is still open

2026-07-29 | Day 4 | 02_grasp_env | **Run 2 is instrumentation only — zero physics knobs turned.** Discriminator: net (unfiltered) pad force printed alongside filtered. net>0 & filtered=0 → the cube filter is not resolving (one knob: `filter_prim_paths_expr`). net=0 & filtered=0 → the pads touch nothing while the fingers are blocked, so the blockage is the r1/l1 links — **the wedge** (one knob: cube placement along the tool axis). Deciding by measurement instead of argument, because two of the three hypotheses would each justify a different knob. | pending run

2026-07-29 | Day 4 | 02_grasp_env | **GUI capture closes the diagnosis: it is the wedge, and the cause is my placement.** The cube sits in the **throat** of the hand, pinched corner-first between the curved r1/l1 links and tilted ~15°, with the r2/l2 pads splayed below it touching nothing. One image explains all four run-1 symptoms: blocked 17 mm wide, zero pad force, 10 mm settle on release, rigid ride through the lift. Figure: `Thesis_Documentation/assets/02_day4_run1_wedge.png` | hypothesis 3 confirmed, 1 and 2 were bookkeeping

2026-07-29 | Day 4 | 02_grasp_env | **The §9 Robotiq failure did NOT reproduce.** Run 1's `[FAIL] pads transmit real normal force` was the script blaming the sensor for a placement bug — the pads were never given anything to push against, so 0.00 N was the *correct* reading. Recording this explicitly because a false §9 positive would have argued for abandoning the RH-P12-RN, the one gripper on the approved list. | false alarm, retracted

2026-07-29 | Day 4 | 02_grasp_env | Fix: the grasp centre is the midpoint of the two pad **geometric** centres, from the link-local AABB via `ComputeUntransformedBound` — the same machinery Day 3 validated in `make_ur5e_rhp12_usd.py: pad_local_bounds`. No new method, no new constant. Script prints `face - origin` so the size of run 1's misplacement is on the record. Added `--place-at {padface,origin}`; `origin` reproduces the wedge exactly, because a negative result that cannot be re-run is an anecdote. | **run 2 turns exactly one knob: cube placement**

2026-07-29 | Day 4 | 02_grasp_env | Run-2 predictions banked: (1) `face - origin` 20–40 mm; (2) stall near the Day 3 q ≈ 0.69 at a face gap near 0.0480 m; (3) filtered pad force non-zero — and if net>0 while filtered=0, the diagnosis moves cleanly to the cube filter, which is the NEXT knob, not this one; (4) TCP_OFFSET near 0.10 m, inside Day 3's 0.0767–0.1049 band, run 1's 0.2258 m void; (5) lift slip still the genuine unknown — a real pad grip may hold *worse* than the jam did. | written before the run

2026-07-29 | Day 4 | 02_grasp_env | **Run 2: FAIL 4/8 — and it refutes my own diagnosis.** One knob turned (cube placement, 12.62 mm lower). Cube fell **358.92 mm** to `z=0.0240` — exactly its half-edge on the ground, so never jammed and never gripped. But the fingers **still stalled at a 0.0690 m face gap, 21 mm wide of a cube they were provably not touching**, lag reaching 0.2741 rad, net force zero on both pads. Log: `logbook/02_grasp_hold_test_run2.log` | placement was NOT the variable

2026-07-29 | Day 4 | 02_grasp_env | **Two fixes confirmed good.** (1) `TCP_OFFSET = (+0.00000, +0.00834, +0.10263)`, magnitude **0.10297 m** — inside Day 3's 0.0767–0.1049 band and within 1 mm of the banked ~0.1015 prediction. The two-instant bug is dead; run 1's 0.2258 m is void. (2) The 8.78 mm pin sag is gone — per-step tracking is a constant 0.681 mm readback lag, benign. | instrumentation earned its place

2026-07-29 | Day 4 | 02_grasp_env | Prediction missed: `face - origin` came out **12.62 mm** against a banked 20–40 mm. The pad AABB centre sits much closer to the joint axis than the GUI capture suggested. | recorded as a miss

2026-07-29 | Day 4 | 02_grasp_env | **The real signal, from the lag column alone.** `τ = k·lag` with k=100: a 0.274 rad lag demands 27.4 N·m against a 10 N·m cap, so **the finger joints are sitting on their effort limit**. Something resists with the full 10 N·m in a gripper that closed to a 0.0216 m origin gap on Day 3. The difference from Day 3 is not the cube — Day 3 teleported the joints, this drives them through a PD loop. | new hypothesis: the obstruction is internal

2026-07-29 | Day 4 | 02_grasp_env | **Run 3 = the control experiment that should have been first.** `--no-cube` removes the cube from the scene entirely (not parked elsewhere — a control removes the variable, it does not relocate it). Also added, both measurements not knobs: contact sensors on **r1/l1** as well as r2/l2 so "which link is touching" becomes a column, and `enabled_self_collisions` printed in the header. If self-collision is live, adjacent finger links' convex-decomposition colliders interpenetrate at their shared joint and PhysX shoves them apart — stalling the gripper at a fixed opening regardless of the cube, which is exactly the symptom. `--grasp-depth` added but defaults to 0. | pending

2026-07-29 | Day 4 | 02_grasp_env | Prediction banked for run 3: **the empty gripper stalls in the same place, ~0.078 m origin gap.** If so, the cube was never the variable, both placement diagnoses were wrong, and the knob is inside the gripper — self-collision first, then `effort_limit_sim`. If instead it closes cleanly to Day 3's 0.0216 m, the cube IS the obstruction, the proximal-link force column says so directly, and `--grasp-depth` is the one knob. | written before the run

2026-07-29 | Day 4 | method | **Method lesson, for the write-up.** I turned a knob twice on a diagnosis reached by argument, and the control run that would have settled it in ninety seconds was never written. The lesson is not that the wedge hypothesis was wrong — it is that **a control experiment is cheaper than a correct hypothesis**, and the rule against changing two things at once does not protect you if you are confidently changing the wrong one thing. | logged for 07_Troubleshooting.md

2026-07-29 | Day 4 | 02_grasp_env | **Run 3 (CONTROL, `--no-cube`): the empty gripper is PERFECT — and my prediction was wrong.** Lag flat at −0.0002 rad across the whole stroke, `q_meas` tracking to 1.0002, closing to origin gap **0.0213 m** / face gap **0.0132 m**, matching Day 3's kinematic sweep (0.0216 / 0.0137) to under a millimetre. `self collisions: False` confirmed in the header. Zero contact on all four links. **The gripper, the PD gains, the 10 N·m effort limit and the config are all vindicated.** Log: `logbook/02_grasp_hold_test_run3_control.log` | self-collision + effort hypotheses both dead

2026-07-29 | Day 4 | 02_grasp_env | **ROOT CAUSE FOUND, by arithmetic.** A 0.048 m cube has a face diagonal of **0.0679 m**. Run 2 stalled at a face gap of **0.0690 m** — within 1.1 mm. **The pads were closing onto the cube's DIAGONAL, not its face.** Runs 1 and 2 both pinned the cube with the identity quaternion (world-axis-aligned) while the gripper hangs at the wrist's orientation. The cube was presented corner-first from the very first run. | one bug, four runs

2026-07-29 | Day 4 | 02_grasp_env | The corner-grip explanation closes every loose end at once: the 21 mm "early" stall was the fingers closing correctly onto the widest section of a rotated cube; zero pad force is a corner touching off the flat inner face; the instant drop on release is a corner grip having nothing to hold with; run 1's GUI tilt of ~15° was the **cause**, which I read as a symptom; and run 1 held only because its cube jammed in the throat where run 2's, 12.6 mm lower, had nothing to jam against. | diagnosis complete

2026-07-29 | Day 4 | 02_grasp_env | **Run 4: one knob — `pin_cube_quat` carries the wrist orientation**, so the cube's faces are square to the pads. Checked, not asserted: PHASE 0 measures the pads' actual separation direction from body positions, compares it to the wrist's local +y (URDF: joint origins at y=±0.008, axes along x) and PASS/FAILs at 5°. Added a "stall width is closer to the face diagonal than to the edge" warning so this mistake announces itself. Predictions: angle < 5°; stall near face gap 0.0480 m i.e. q ≈ 0.69; filtered force non-zero and same order as 170 N; static hold passes; lift slip still the genuine unknown. | pending

2026-07-29 | Day 4 | method | **The real result of the day.** Three runs, three hypotheses — proximal wedge, placement depth, internal self-collision — all mine, all reached by argument from a table, all wrong. What broke the loop was a **control**: remove the object entirely and check the mechanism works at all. Ninety seconds. The confirming evidence was then arithmetic (0.048 × √2 = 0.0679) available since Day 3. `face - origin`, `--grasp-depth` and the proximal sensors were all built to chase hypothesis 2; none were wrong to add, none found this. **Run the control first.** | for 07_Troubleshooting.md

2026-07-29 | Day 4 | 02_grasp_env | **Run 4: orientation fix CONFIRMED.** `pad closing axis [-1.000 -0.000 +0.032]` vs `wrist local +y [-1.000 -0.000 +0.032]`, **angle 0.00 deg**, PASS. The cube is square to the pads. | hypothesis closed

2026-07-29 | Day 4 | 02_grasp_env | **But the corner-grip theory is dead too.** With a square cube the stall is still at a 0.0695 m face gap — so the 0.0679 m face-diagonal match was a coincidence of arithmetic, not the mechanism. Being right about the orientation did not make me right about the cause. | second hypothesis refuted by its own fix

2026-07-29 | Day 4 | 02_grasp_env | **THE WEDGE, MEASURED AT LAST.** Peak net contact force: pads r2/l2 **0.0 N**, proximal r1/l1 **589.8 N**. The proximal links carry the entire load and the pads carry none. Same conclusion I asserted twice without evidence — now with a number behind it. The cube rides the throat, and the earlier 12.62 mm placement correction moved it nowhere near far enough. The r1/l1 sensors, added as instrumentation, are what found it. | root cause confirmed

2026-07-29 | Day 4 | 02_grasp_env | Reporting bug in the same run: `peak_net` maxed over BOTH the pad and proximal columns, inherited the 589.8 N, and printed the wrong diagnosis branch ("the pads ARE in contact"). The raw table underneath was correct throughout — the argument for always printing raw columns next to a derived verdict. Fixed. | my bug, caught by my own table

2026-07-29 | Day 4 | 02_grasp_env | **Run 5: `--place-at tip`, now the default — grasp at the FINGERTIP.** Cube's outer face flush with the distal end of the pads, 2 mm margin: `tip_mid = wrist + tool_axis * (pad_reach_along_tool - cube_edge/2 - 0.002)`. `pad_reach_along_tool` is MEASURED — the 8 corners of each pad's link-local AABB transformed to world and projected onto the tool axis, read at the pose it is used at because the pads rotate with q. Also the physically right answer independent of debugging: a parallel gripper holds at the fingertips, where the faces are flat, the finger moment arm is smallest, and the ROBOTIS 170 N is specified. `padface` and `origin` kept so every earlier failure stays reproducible. | pending

2026-07-29 | Day 4 | 02_grasp_env | Run 5 predictions: (1) `tip - padface` 20–40 mm further out; (2) proximal force → ~0 N and the PADS become the loaded pair; (3) stall near face gap 0.0480 m i.e. q ≈ 0.69, Day 3's prediction on its first genuinely fair test; (4) filtered pad-to-cube force non-zero — and if `Nr2>0` while `Fc_r2=0` that is the cube filter, the NEXT knob not this one; (5) static hold passes, lift slip still the honest unknown. | written before the run

2026-07-29 | Day 4 | 02_grasp_env | **Run 5 (`--place-at tip`): `tip - padface` = 1.39 mm.** Predicted 20–40 mm. The pad is ~53 mm along the tool axis and the cube is 48 mm, so the cube already fills the pad end to end — "flush with the tip" and "centred on the face" are practically the same placement. There was never 20 mm of room. Load unchanged: **peak 654 N on r1/l1, 0 N on r2/l2**. Log: `logbook/02_grasp_hold_test_run5_tip.log` | fourth wrong geometric prediction today

2026-07-29 | Day 4 | 02_grasp_env | **Pattern, stated plainly.** Four geometric predictions today, all wrong: the proximal wedge (right conclusion, unmeasured reasoning), `face - origin` 20–40 vs 12.62 mm, empty gripper stalls vs closes perfectly, `tip - padface` 20–40 vs 1.39 mm. Every one an inference from joint origins and a screenshot. The two things that actually advanced the diagnosis were a **control run** and a **contact sensor on a link I had assumed irrelevant**. On this hand: do not reason about geometry, instrument it. | the real methodological result of Day 4

2026-07-29 | Day 4 | 02_grasp_env | **The question nobody has measured: is r2/l2 even the gripping surface?** `PAD_BODIES` was set to the distal links on assumption and never checked. The URDF puts r2's origin **49.4 mm outboard** of r1's — at q=0 the r2 origins are 114.7 mm apart while the r1 origins are 16 mm apart. If r1/l1 are the inner surface, this script has had the two links' roles backwards since it was written, "pads read 0 N" has been the expected result all along, and every placement fix was aimed at the wrong geometry. | open, and decisive

2026-07-29 | Day 4 | 02_grasp_env | **PHASE 0b added — the measurement that should have existed on Day 3.** Free space, no cube, no contact: at each q, the closest approach of all four finger links to the centreline, along the measured closing axis, from the 8 corners of each link's AABB transformed to world at that pose. Smallest number = the link a centred cube meets first; the q where 2× it crosses 0.048 m is where the cube is captured and by which link. Pure geometry — cannot be confounded by placement, orientation, contact filtering or the effort limit, each of which has already sent me down a wrong path once. | pending

2026-07-29 | Day 4 | 02_grasp_env | Prediction for PHASE 0b, banked with deliberately low confidence given the day's record: the r1/l1 columns are smaller than r2/l2 over most of the stroke, and `PAD_BODIES` names the wrong links. | written before the run

2026-07-29 | Day 4 | 02_grasp_env | Run 6 first attempt crashed: `pad_idx` built from `PAD_BODIES` only, `KeyError: rh_p12_rn_r1`. One-line fix, no physics involved. | my bug

2026-07-29 | Day 4 | 02_grasp_env | **SOLVED — and nothing was ever broken.** PHASE 0b (free space, no cube, no contact) measured the closest approach of each finger link to the centreline. **The proximal links never open wider than 39.9 mm** (peak at q=0.90); they converge faster than the fingertips. So any object above ~40 mm is captured by the throat before the pads can close on it — at every q, regardless of placement, orientation or grip force. Log: `logbook/02_grasp_hold_test_run6_linkgeom.log` | root cause: gripper kinematics, not a defect

2026-07-29 | Day 4 | 02_grasp_env | Cube vs gripper, measured: 48 mm → pads reach at q=0.69 but proximal are 31.5 mm apart (throat first); 40 mm → 34.6 mm (throat first); 35 mm → clear by 1.5 mm; **30 mm → clear by 8.2 mm**; 25 mm → clear by 14.8 mm. | the table that ends the debugging

2026-07-29 | Day 4 | 02_grasp_env | **Day 3's banked q ≈ 0.69 was CORRECT.** The pads really do reach a 48 mm cube there. What Day 3 could not know — it only measured the pads — is that the proximal links arrive first. A right prediction from which a wrong conclusion followed; worth its own sentence in the write-up. | nuance recorded

2026-07-29 | Day 4 | 02_grasp_env | **Decision: 30 mm cube, `--cube-scale 0.5`, 8.2 mm clearance.** 35 mm is the largest that clears but by only 1.5 mm, and Day 3 already flagged the pad AABB as over-approximating a curved fingertip — that margin sits inside our own error bar. Defaults updated: `--cube-scale 0.5`, `--predicted-q 0.85`. **Must be stated in the methods chapter** — it changes the task relative to the archive (0.8 → 48 mm). Justification is external and strong: object size set by the gripper's measured kinematics, not chosen for convenience. | task parameter fixed by measurement

2026-07-29 | Day 4 | 02_grasp_env | Run 7 predictions (30 mm cube): proximal force → ~0 N and the PADS become the loaded pair (the swap that has failed to appear for five runs); stall near q=0.85, face gap near 0.030 m; filtered pad force non-zero and same order as 170 N (if `Nr2>0` while `Fc_r2=0` that is the cube filter, the next knob); static hold passes; lift slip still the honest unknown but for the first time a real fingertip grip under test. | written before the run

2026-07-29 | Day 4 | method | **The day in one line.** Six runs. Five wrong hypotheses, all mine, all reached by reasoning about geometry from joint origins and screenshots. Two things worked: a **control run**, and **measuring the geometry directly instead of arguing about it**. The final answer was nine rows of numbers that could have been produced on Day 3 in ten minutes. | for 07_Troubleshooting.md and the methods chapter

2026-07-29 | Day 4 | 02_grasp_env | **Reverted to the 48 mm cube (`--cube-scale 0.8`) at the supervisor's instruction, and the run-6 analysis is now flagged SUSPECT.** PHASE 0b's "proximal links never open wider than 39.9 mm" came from AXIS-ALIGNED BOXES around CURVED links. An AABB reports its inner face at every height — including where the real arm has swung far outboard — so those numbers are an UPPER bound on how close the proximal links really come, not a measurement of it. Same class of error as the Day 3 `size/2` bug and as five other geometric inferences today. | conclusion withdrawn pending direct measurement

2026-07-29 | Day 4 | 02_grasp_env | **Switched to PhysX contact POSITIONS — `track_contact_points=True`, `contact_pos_w`.** New `depth` column: the loaded contact's position projected onto the tool axis, in mm below the wrist origin, compared against the measured `pad reach along tool` (fingertip distance). Small = held in the throat, near the fingertip value = held at the tip. No geometry inference at all — PhysX already knows where the contact is. | the measurement that ends the argument

2026-07-29 | Day 4 | 02_grasp_env | Also now printing FILTERED force for the proximal links (`Fc_r1`/`Fc_l1`), which was never printed before. "The cube filter is not resolving" was therefore never actually tested — the pads simply were not touching. If `Fc_r1 > 0` the filter is fine and that whole branch of suspicion dies. | gap in the instrumentation closed

2026-07-30 | Day 5 | 02_grasp_env | **Second gripper opened at the supervisor's instruction, ahead of the Day 4 handoff's "do not start it this session".** Recording the override explicitly: Module 02's critical path is still open — run 7 unrun, run 6's PHASE 0b analysis withdrawn as SUSPECT, pad-vs-proximal load on the RH-P12-RN still unmeasured. The 2F-85 timebox of ONE day stands and Layer 1 is not gated on it. | scope decision, logged not buried

2026-07-30 | Day 5 | 02_grasp_env | **The archive was mined and it paid.** `~/Abdur_Rabbi_THESIS` confirms `ur5e.usd` carries a variant set `Gripper=[None, Robotiq_2f_85]`, its merged USD loaded as ONE articulation at 12 joints / 16 bodies, and it names all six finger joints. Caveat that matters: the archive read that asset from `Assets/Isaac/**5.1**/Isaac` and this thesis is frozen on **5.0**. Every name carried across is a HYPOTHESIS until PHASE C re-reads it here. | §14 earning its place a second time

2026-07-30 | Day 5 | 02_grasp_env | **The archive's 2F-85 failure has a precise, avoidable cause — and it is not the one §9 states.** §9 rejects the URDF route (a tree cannot hold a four-bar loop, so public URDFs fake it with `<mimic>`). The archive did not use a URDF. Its `ur5e_robotiq.py` drove `finger_joint` alone and set the other five finger joints PASSIVE at stiffness 0, expecting the mechanical loop to carry them; the pads then transmitted no normal force and the cube fell through. Raising stiffness 20→400 and effort 50→200 did not help, and it fell back to a proximity WELD — the fake gripper that measured every headline number in the previous thesis. | root cause of a two-week-old failure, named

2026-07-30 | Day 5 | 02_grasp_env | **Why passive cannot work, from upstream rather than from me.** Isaac Sim resolves closed-loop kinematics automatically through USD schemas, but **Isaac Lab requires every mimic joint to be fully specified in the ArticulationCfg** (IsaacLab #2424, #2626, #2665). A coupled joint Isaac Lab was never told about is not coupled — it is limp, and limp joints fold under contact instead of transmitting it. That is "pads touch, force ≈ 0 N" exactly. | external source, not inference

2026-07-30 | Day 5 | 02_grasp_env | **Design: all SIX finger joints DRIVEN from one scalar through an explicit sign table, none passive.** Not a new idea — it is the pattern that already worked in this folder on the RH-P12-RN, where four coupled joints sharing one scalar target reproduced the ROBOTIS published 106.0 mm to +0.4 mm. The signs live in the binary action command, not in the actuator gains, so the policy still sees ONE scalar and the two grippers stay benchmark-comparable. | the whole bet of the 2F-85 track

2026-07-30 | Day 5 | 02_grasp_env | Written, none run: `ur5_grasp/tools/make_ur5e_robotiq_usd.py` (PHASES A–F), `ur5_grasp/robots/ur5e_robotiq_cfg.py`, `GRIPPERS` registry in `robots/__init__.py`. Regex coverage checked offline: all 12 joints matched by exactly one actuator group, no overlaps. Nothing here is a result. | files on disk, zero measurements

2026-07-30 | Day 5 | 02_grasp_env | **Day 4's lesson built into the tool rather than written under it.** PHASE E measures the free-space closest approach of EVERY finger link to the centreline before anything touches anything — the nine rows of numbers that would have saved six runs on the RH-P12-RN. `PAD_BODIES` is carried as a labelled hypothesis with a test attached, not as a fact, because that exact assumption went unchecked for two days on the other hand. | instrument it, do not argue about it

2026-07-30 | Day 5 | 02_grasp_env | External validation target banked BEFORE the run: Robotiq publishes stroke **85 mm** and gripping force **20–235 N**. PHASE F checks the open clear opening against 85 mm ±4 mm, the same move that made the RH-P12-RN's 106.4 mm worth something. Effort limit set to 12.0 N·m from 235 N × ~0.05 m and labelled PROVISIONAL — calibrate against the measured FORCE, not the torque. The archive's 200.0 is ~17× the datasheet and is rejected. | prediction written before the run

2026-07-30 | Day 5 | 02_grasp_env | Upstream issue noted for the grasp test, not chased: IsaacLab #3385 reports 2F-85 grasping inducing consistent whole-arm rotation, worse at higher finger PD gains. If the arm twists on close, that is this, not the reward. | landmine banked

2026-07-30 | Day 5 | method | **CORRECTION, and the rule that caught it.** The Day 4 handoff said run 7 was unrun, and the first draft of the Day 5 handoff repeated it. `ls` says otherwise: `02_grasp_hold_test_run7_30mm.log` (14:21) and `02_grasp_hold_test_run7_depth.log` (14:26) are both on disk. §5's "trust `ls` and `git status` over any handoff, including this one" has now been vindicated by the very handoff that states it. Chased the error into `00_INDEX.md` and `HANDOFF_next.md` in the same pass. | false claim retracted before it propagated further

2026-07-30 | Day 5 | 02_grasp_env | **Run 7 has FAILED TWICE, and the failure moved.** 30 mm cube: FAIL 2/9 — stall q off the banked 0.85, pads transmit no real normal force; lift itself passed (+84.2 mm, slip 0.15 mm). 48 mm cube with the `depth` column: FAIL 3/9 — **stall q measured 0.3063 against the banked 0.69 (error −0.3837)**, **peak pad force 0.00 N**, static hold dropped **+32.31 mm**. Passed in both: all 4 finger prims resolved, all 4 joints found by name, cube square to the pads (0.00°), open pads clear the cube (0.1066 m vs 0.0480 m), TCP rose, slip < 0.2 mm. | the critical path, still open

2026-07-30 | Day 5 | 02_grasp_env | **The stall number is the story and it is new.** 0.31 against a predicted 0.69 is not a near miss — the gripper is stopping at roughly HALF the commanded stroke. Neither the pad theory nor the throat theory predicts that. Run 6's PHASE 0b stays WITHDRAWN (AABBs around curved links), so "the throat captures first" is unproven, not disproven. **Next diagnostic is the `depth` column — where the loaded contact sits along the tool axis — and NOT another argument from joint origins.** Do not tune grip force to escape this. | open question, sharpened
